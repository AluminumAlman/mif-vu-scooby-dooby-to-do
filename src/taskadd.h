#ifndef TASK_ADD_H
#define TASK_ADD_H

#include "common.h"

extern char TaskAdd(char argumentFile[], Task allTasks[], int newTaskIndex); //function returns 1 if task is successifully added, 0 if error occured. 


#endif
