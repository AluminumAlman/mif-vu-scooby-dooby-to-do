#ifndef TASK_MARK_H
#define TASK_MARK_H

#include "common.h"

extern char TaskMark(char taskName[], Task taskList[], int taskAmount);   //if task is successfully marked, then function returns 1, otherwise 0. 

#endif
